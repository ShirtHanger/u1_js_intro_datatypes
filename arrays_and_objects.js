// Arrays are a list of variables

let cheeses = ['brie', 'gouda', 'edam', 'pepperjack']

// Objects are stored in {}
// They contain differerent types of data, and used throughout the web
// key: value

const user = {
    username: 'fakeuser123',
    pass: 'password123',
    id: 1273,
    isLoggedIn: false,
    favCheeses: cheeses
}

console.log(user) // Prints entire object